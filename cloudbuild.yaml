steps:
  # 1. Pull the previous image to use as cache source for Docker layer caching
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        docker pull $_IMAGE:latest || exit 0
    id: 'pull'

  # 2. Build the new container image, using the previous image to cache layers
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--cache-from'
      - '$_IMAGE:latest'
      - '-t'
      - '$_IMAGE:latest'
      - '-t'
      - '$_IMAGE:$BUILD_ID'
      - '.'
    id: 'build'
    waitFor: ['pull']

  # 3. Push the images to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_IMAGE:latest']
    id: 'push-latest'
    waitFor: ['build']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_IMAGE:$BUILD_ID']
    id: 'push-sha'
    waitFor: ['build']

  # 4. Prepare YAML configurations — inject dynamic RELEASE_VERSION from build timestamp
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        # Stamp the docker image tag
        sed -i "s|web-app:latest|web-app:$BUILD_ID|g" service.yaml
        sed -i "s|web-app:latest|web-app:$BUILD_ID|g" service-worker.yaml
        # Inject a live RELEASE_VERSION = <date>-<short-sha> so the UI always shows the correct build
        RELEASE_STAMP="$(date -u +%Y%m%d-%H%M%S)-${BUILD_ID:0:8}"
        sed -i "s|value: \"[0-9]\{8\}-[0-9]\{6\}\"|value: \"$RELEASE_STAMP\"|g" service.yaml
        sed -i "s|value: \"[0-9]\{8\}-[0-9]\{6\}\"|value: \"$RELEASE_STAMP\"|g" service-worker.yaml
        echo "RELEASE_VERSION set to: $RELEASE_STAMP"
    id: 'prepare-yaml'
    waitFor: ['build']

  # 5. Deploy the web service
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'services'
      - 'replace'
      - 'service.yaml'
      - '--region=asia-southeast1'
    id: 'deploy-web'
    waitFor: ['push-sha', 'prepare-yaml']

  # 6. Deploy the worker service
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'services'
      - 'replace'
      - 'service-worker.yaml'
      - '--region=asia-southeast1'
    id: 'deploy-worker'
    waitFor: ['push-sha', 'prepare-yaml']

substitutions:
  _IMAGE: 'asia-southeast1-docker.pkg.dev/cs-poc-pfjoxqspipkzqdfmgnqcd3c/data-extractor-repo/web-app'
